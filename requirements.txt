# Core Dependencies
numpy>=1.21.0
python-dateutil>=2.8.2
pyyaml>=6.0

# For agent-memory (SQLite)
# Built-in, no additional dependency needed

# Optional: For better performance
# sqlite3 is included in Python standard library

# Causal Inference
pyyaml>=6.0
